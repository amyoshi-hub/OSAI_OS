usb へのinstall

sudo parted /dev/sda
(parted) mklabel msdos                                                    
Yes/No? yes                                                               
(parted) quit                                                             

sudo parted /dev/sda
(parted) mkpart primary fat32 1MiB 100%
(parted) set 1 boot on
(parted) quit

sudo mkfs.vfat -F32 /dev/sda1

sudo apt install grub-pc


sudo grub-install --target=i386-pc --boot-directory=/mnt/boot --directory=/usr/lib/grub/i386-pc /dev/sda


## grub.cfgを書き込む
└─$ cat /mnt/boot/grub/grub.cfg
set timeout=5
set default=0

menuentry "OSAI Linux" {
    linux /boot/bzImage root=/dev/ram0 console=ttyS0
    initrd /boot/initramfs.cpio.gz
}


起動
sudo qemu-system-x86_64 -drive file=/dev/sda,format=raw -serial stdio

